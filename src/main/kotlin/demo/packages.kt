package demo

import kotlin.math.PI as circleRatio
import kotlin.math.cos as cosine
import kotlin.math.* // import everything

fun mathTest() {
    println(circleRatio)
    println(cosine(circleRatio))
    println(cosine(2 * circleRatio))
}